# ── ML Flask API ─────────────────────────────────────────
# Fix 20: Set this to match ML_API_KEY in backend/.env
ML_API_KEY=change_me_to_a_strong_shared_secret

# ── Flask ────────────────────────────────────────────────
FLASK_ENV=development
# PORT is hardcoded to 5000 in app.py
